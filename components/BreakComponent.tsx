import React from 'react';

interface Props {
	marginTop?: string;
	marginBottom?: string;
}

const BreakComponent = ({
	marginTop,
	marginBottom,
}: Props) => {
	return <div></div>;
};

export default BreakComponent;
